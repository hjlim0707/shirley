import React from 'react';
import ReactDOM from 'react-dom';
import HomeHeader from '../../components/home_header';

export default class PermissionCheckForm extends React.Component {
  state = {
    currPassword: '',
  };

  render() {
    <div>
      <HomeHeader />
    </div>
  }
};
